{
  "name": "english-reading-pwa",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.html dist/index.html && cp -r public/* dist/ 2>/dev/null; cp src/components/*.jsx dist/ 2>/dev/null; echo done"
  }
}
