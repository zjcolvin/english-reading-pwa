{
  "name": "english-reading-pwa",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.html dist/ && cp -r public/* dist/ 2>/dev/null; echo done"
  }
}
